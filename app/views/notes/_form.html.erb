<%= form_for([@appointment, @appointment.notes.build]) do |f| %>
  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_area :description %>
  </div>
  
  <div>
    <%= f.submit %>
  </div>
<% end %>
