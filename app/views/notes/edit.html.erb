<h1>Edit Note</h1>
<%= form_for([@appointment, @note]) do |f| %>

  <div>
    <% if @note && @note.errors.any? %>
      <% @note.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
      <% end %>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_area :description %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>
